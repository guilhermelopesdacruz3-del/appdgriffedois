# URL pública do proxy (veja /server). NUNCA coloque chave_aplicacao ou
# chave_api aqui — variáveis VITE_* ficam visíveis no navegador do visitante.
VITE_LOJA_INTEGRADA_PROXY_URL=http://localhost:8787/api/loja-integrada
